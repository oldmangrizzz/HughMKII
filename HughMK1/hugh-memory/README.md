# HUGH Memory - Quick Deploy

```bash
cd ~/workspace/HughMK1/hugh-memory
npm install
npx convex login
npx convex dev
```

Save the deployment URL to:
```bash
export HUGH_CONVEX_URL="<url-from-output>"
```

Done! HUGH now has persistent memory across all devices.
